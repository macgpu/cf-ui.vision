@{
    ViewBag.Title = "visionFind - Visual UI Testing and Monitoring in Chrome and Firefox";
}

<div id="content">
    <!--  CONTENT   -->
    <div id="page-header" class="page-extension">
        <div class="row">
            <div class="span12 whitebg">
                <h1><span>visionFind</span> - Visual UI Testing</h1>
            </div>
        </div>
    </div>

    <div class="row">
        <div class="span12">
            <h3 class="headline">
                <span>visionFind | IMAGE name@@0.89 | variable for number of matches)</span>
            </h3>
        </div> 

        <div class="span12">

            <p>visionFind is now called <a href="/rpa/docs/visual-ui-testing">visualSearch</a> and part of our suite of 
                <a href="/rpa/docs/visual-ui-testing">visual ui testing</a> commands.</p>

<p>The  <span class="hired">visionFind</span> command allows you to write automated visual tests with <em>Ui.Vision RPA</em> - this makes <em>Ui.Vision RPA</em> the first and only Chrome and 
            Firefox extension that has "👁👁 eyes". A huge benefit of doing visual tests is that you are not just checking one element or two elements at a time,
            you’re checking a whole section or page in one visual assertion. </p>

            <p>visionFind will take a screenshot of the page, and search it against the provided image. If the comparison says that the new image is found on the screenshot, then visionFind succeeds, otherwise it fails. You can supply a confidence value behind the @@ symbol.
            Its value ranges from 1 (exact match) to 0.1 (almost everything matches). visionFind waits 
                <a href="/rpa/docs#!timeout_wait">!timeout_wait</a>
                    seconds for the image to appear. If the image is not found, an error is triggered (you can overwrite this
                    with <a href="/rpa/docs#!errorignore">!errorignore</a>). The number of matches is filled into a variable, just like the
                    <a href="/rpa/docs/selenium-ide/sourceSearch">sourceSearch</a> command does.
</p>


           

            <p>
                <img src="~/Content/Images/visionfind.webp" alt="Visual Testing and Monitoring" class="responsive-img">
            </p>
<p><span class="hired">visionFind runs 100% local in your web browser.</span> It uses C++ powered computer-vision image processing,
    running directly in your Chrome and Firefox as WebAssembly code. The input images for visionFind are stored
            in the "Vision" tab:
</p>

            <p>
                <img src="~/Content/Images/visionfind-tab.webp" alt="Visual testing and visual assert with the new visionFind command" class="responsive-img">
            </p>
            <p>When you select <em>visionFind</em> the normal Selenium IDE "Select" and "Find" buttons turn into "👁 Select" and "👁 Find".   "👁 Select" allows you to take new screenshots for use with visionFind,  and
             "👁 Find" starts an image search. The found areas are marked along with their confidence value. The best match is marked with a pink frame. 
            </p>


            <h3 id="visionLimitSearchArea">visionLimitSearchArea (viewport/full)</h3>

            <p>The visionLimitSearchArea allows you to change the search area from viewport (the part of the website that is currently visible in the browser)
            to full page.</p>

            <h3 id="efp">Click on Elements with #elementFromPoint(${!imageX}, ${!imageY})</h3>

            <p>The coordinates of the <em>center</em> of the best found match are available in the internal variables <em>!imageX</em> and <em>!imageY</em>.
            Together with the elementFromPoint (x,y) function of the web browser, you can find out which element (locator) is at the image, and then use this locator
            as input for all commands that take a locator as input. For the most common use of elementFromPoint with the !imageX/Y we introduced a shortcut notation of just using <em>#efp</em>, which of course e-f-p stands for e_lement_f_rom_p_oint.
                </p>
            <p>
                <table class="table table-bordered">
                    <thead>
                    <th class="col-md-2">Command</th>
                    <th class="col-md-5">Target</th>
                    <th class="col-md-5">Comment</th>
                    </thead>
                    <tbody>

                        <tr>
                            <td>visionFind</td>
                            <td>(image name here)</td>
                            <td>This sets !imageX and !imageY if image is found</td>
                        </tr>
                        <tr>
                            <td>Click</td>
                            <td class="success">#elementFromPoint (<em>${!imageX}</em>, <em>${!imageY}</em>)</td>
                            <td>comment: same as using #efp, see below:</td>
                        </tr>
                        <tr>
                            <td>Click</td>
                            <td class="success">#efp</td>
                            <td>shorthand notation</td>
                        </tr>
                        <tr>
                            <td>Type</td>
                            <td class="success">#efp</td>
                            <td>elementFromPoint can be used with any command that takes a locator as input</td>
                        </tr>
                        <tr>
                            <td>Click</td>
                            <td class="success">#elementFromPoint (<em>${anyOtherVariable}</em>, <em>${var2}</em>)</td>
                            <td>Here we need to full command as we use different variables as input.</td>
                        </tr>

                    </tbody>
                </table>
            </p>
            <p>
                Note that the elementFromPoint function of the browser is not always reliable, on complex websites with overlapping elements it might return an element
                that can not receive a click. If you find such cases, report them to us in the <a href="https://forum.ocr.space/">forum</a>, there are probably workarounds available.
                           </p>


                <h3>visionFind Example</h3>

            <p>Quite literally, seeing is believing. The best way to understand visionFind is to run the included DemoComputerVision macro. It is installed
            with every fresh <em>Ui.Vision RPA</em> install and shows a visual test of the <a href="https://ocr.space">best online OCR</a> website.</p>
            <p>
                <table class="table table-bordered">
                    <thead>
                    <th class="col-md-2">Command</th>
                    <th class="col-md-5">Target</th>
                    <th class="col-md-5">Pattern/Text</th>
                    </thead>
                    <tbody>

                        <tr>
                            <td>open</td>
                            <td>https://ui.vision/</td>
                            <td></td>
                        </tr>
                        <tr class="success">
                            <td>visionFind</td>
                            <td>buttonImage</td>
                            <td>matches</td>
                        </tr>
                    </tbody>
                </table>
            </p>

            <h3>Works in</h3>
            <p>
                <em>Ui.Vision RPA</em> for Chrome <a href="/">Selenium IDE</a>,  <em>Ui.Vision RPA</em> for Firefox <a href="/">Selenium IDE</a>,   <a href="/howto/getseleniumide/">Firefox IDE Classic</a>
            </p>

            <h3>Related Demo Macros</h3>
                <p>DemoComputerVision</p>

                <h3>See also</h3>
                <p><a href="/rpa/docs/selenium-ide/storetext">storeText</a>,

                    <a href="/rpa/docs/">Web Automation Extension</a> User Manual,

    <a href="/rpa/docs/selenium-ide/">Selenium IDE commands</a>, <a href="/rpa/docs/selenium-ide/oldfirefoxide">Classic Firefox Selenium IDE</a>.
</p>


                <h3>Anything wrong or missing on this page? Suggestions?</h3>
                <p>...then please <a href="/contact">contact us</a>.</p>

            <a href="/">
                <img src="~/Content/Images/Selenium-IDE-Chrome1.webp" alt="<em>Ui.Vision RPA</em> Selenium IDE for Chrome and Firefox - Web Test Automation" class="responsive-img">
            </a>
</div>
    </div>
</div>
